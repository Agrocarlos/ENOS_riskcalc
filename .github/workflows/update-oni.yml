name: Actualizar ONI (NOAA CPC)

on:
  schedule:
    - cron: "0 12 12 * *" # día 12 de cada mes (CPC publica ~día 10)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python3 scripts/update_oni.py
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm test
      - name: Commit si cambió
        run: |
          if git diff --quiet data/oni.js; then echo "Sin cambios"; exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/oni.js
          git commit -m "data: actualizar ONI desde NOAA CPC"
          git push
